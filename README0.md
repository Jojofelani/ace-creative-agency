# Ace Creative Agency Website

This is the codebase for the Ace Creative Agency website, built with Next.js, Tailwind CSS, and Framer Motion.

## 🚀 Getting Started

1.  **Install dependencies:**
    ```bash
    npm install
    ```

2.  **Run the development server:**
    ```bash
    npm run dev
    ```

3.  **Open the site:**
    Visit [http://localhost:3000](http://localhost:3000) in your browser.

## 🛠️ Tech Stack

-   **Framework:** Next.js 14+ (App Router)
-   **Styling:** Tailwind CSS (v4 configuration)
-   **Animation:** Framer Motion
-   **Icons:** Lucide React
-   **Fonts:** Outfit (Headings), Inter (Body) - via `next/font/google`

## 📂 Project Structure

-   `src/app`: Pages and layouts
-   `src/components/ui`: Reusable UI components (Buttons, etc.)
-   `src/components/sections`: Homepage sections (Hero, Featured Work, etc.)
-   `src/lib`: Utilities

## 🎨 Design Config

The Tailwind theme is configured in `src/app/globals.css` using CSS variables for:
-   `--color-gold`
-   `--color-blush`
-   `--color-charcoal`
-   `--color-off-white`
